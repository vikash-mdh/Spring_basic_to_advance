package org.example.Collections_8;

public class HashSet {
    public static void main(String[] args) {
//       1. what is HashSet
//        A HashSet in Java is a collection that doesn't allow duplicate elements and doesn't guarantee any
//        specific order of elements. It is part of the java.util package.

//       2. Create a HashSet:
//        HashSet<Type> set = new HashSet<>();
//        HashSet<String> set = new HashSet<>();

//      3. Add elements (add()): You can add elements to a HashSet using the add() method. If the element is
//        already present, it won't be added agai

//        set.add("Hello");
//        set.add("World");
//        set.add("Hello"); // Duplicate, won't be added


//       4.Remove an element (remove()): You can remove an element from a HashSet using the remove() method.
//          set.remove("World");
//
//       5.Check if an element exists (contains()): You can check if an element is present in the HashSet using the contains() method.
//        boolean isPresent = set.contains("Hello"); // Returns true

//       6.Size of the HashSet (size()): You can get the size (number of elements) of the HashSet using the size() method.
//        int size = set.size(); // Returns the number of elements in the set

//       7.Clear the HashSet (clear()): To remove all elements from the HashSet, you can use the clear() method.
//        set.clear();

//       8.Iterate through the HashSet: You can iterate over the HashSet using a for-each loop.
//        for (String s : set) {
//            System.out.println(s);
//        }

//      9.heck if the HashSet is empty (isEmpty()): You can check if the HashSet is empty using the isEmpty() method.
//        boolean isEmpty = set.isEmpty(); // Returns true if empty



    }
}
